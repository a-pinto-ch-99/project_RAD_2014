<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_last_name %><br>
    <%= f.text_area :user_last_name %>
  </div>
  <div class="field">
    <%= f.label :user_first_name %><br>
    <%= f.text_area :user_first_name %>
  </div>
  <div class="field">
    <%= f.label :user_e_mail %><br>
    <%= f.text_area :user_e_mail %>
  </div>
  <div class="field">
    <%= f.label :user_access_rights %><br>
    <%= f.text_area :user_access_rights %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
